# Скомпилированные программы (артефакты сборки) — в репозиторий не попадают.
# Каждый такой файл собирается из .cpp командой: clang++ файл.cpp -o файл
*.o
*.out
*.exe
*.dSYM/
a.out

# macOS
.DS_Store
